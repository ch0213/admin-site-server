package admin.adminsiteserver.member.member.domain;

import org.springframework.data.jpa.repository.JpaRepository;

public interface MemberFilePathRepository extends JpaRepository<MemberFilePath, Long> {
}
