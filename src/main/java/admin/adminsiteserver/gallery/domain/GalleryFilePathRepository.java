package admin.adminsiteserver.gallery.domain;

import org.springframework.data.jpa.repository.JpaRepository;

public interface GalleryFilePathRepository extends JpaRepository<GalleryFilePath, Long> {
}
